<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Collection Comparison</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
  <link rel="stylesheet" href="stylesheets/home.css" type="text/css">

</head>

<body>
  <div class="container pt-3">
    <h1 class="text-center">Collection</h1>
    <h2 class="text-center"><%= main %>'s collection: <%= mainCollection.length %> </h2>
    <h2 class="text-center"><%= compare %>'s collection: <%= compareCollection.length %> </h2>
    <h2 class="text-center">Filtered Amount: <%= filtered.length %> </h2>

    <table class="table table-sm">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Image</th>
          <th scope="col">Title</th>
          <th scope="col">Num Players</th>
          <th scope="col">Playtime</th>
        </tr>
      </thead>

      <tbody>
        <% for( let i = 0; i < filtered.length; i++ ) { %>
        <tr>
          <th scope="row"><%= i+1 %></th>
          <td><img src="<%= filtered[i].image %>" alt="<%= filtered[i].name.$t %>" class="img-fluid"
              style="width:50px;height:50px">
          </td>
          <td><%= filtered[i].name.$t %> </td>
          <td>
            <%= filtered[i].stats.minplayers  %>
            <% if (filtered[i].stats.maxplayers != filtered[i].stats.minplayers) { %>
            - <%= filtered[i].stats.maxplayers %>
            <% } %>
          </td>
          <td>
            <%= filtered[i].stats.minplaytime  %>
            <% if (filtered[i].stats.maxplaytime != filtered[i].stats.minplaytime) { %>
            - <%= filtered[i].stats.maxplaytime %>
            <% } %> mins
          </td>
        </tr>
        <% } %>

      </tbody>

    </table>

  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>
</body>

</html>